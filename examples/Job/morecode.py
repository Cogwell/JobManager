def mangleSomeInput(var, arr):
    varStr = "I just got some var: %s" % repr(var)
    length = len(arr)
    return varStr, length

def getSomeMangledInput(v, l):
    print v
    print l

def sendDict(d):
    print d
